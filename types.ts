export type userType = {
  name: string;
  email: string;
  id: string;
};

export type scoreType = {
  course: string;
  score: number;
  userId: string;
  id: string;
};
export type userTableType = {
  name: string;
  course: string;
  score: number;
  userId: string;
  id: string;
};
